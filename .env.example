# From https://trello.com/power-ups/admin -> your Power-Up -> API Key.
# This key is public by design (it ships in the client bundle). The member's
# token is the secret, and it is only ever stored in Trello's own
# member/private plugin storage — never here.
VITE_TRELLO_APP_KEY=your_trello_api_key_here
